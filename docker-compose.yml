version: '3.4'

services:
  dna-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DEPLOY_ENV=prod
    secrets:
      - dna-frontend-secrets

secrets:
  dna-frontend-secrets:
    external: true
