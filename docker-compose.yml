version: '3.4'

services:
  dna-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DEPLOY_ENV=prod
    env_file:
      - .env.production
